diff --git a/android/src/main/java/expo/modules/maps/GoogleMapsView.kt b/android/src/main/java/expo/modules/maps/GoogleMapsView.kt
index 82450b6ab008ef190cbc499d987ffbc774309c82..b24882d1c062bcd4a654cb4fcfc86bc89243f3e3 100644
--- a/android/src/main/java/expo/modules/maps/GoogleMapsView.kt
+++ b/android/src/main/java/expo/modules/maps/GoogleMapsView.kt
@@ -228,9 +228,11 @@ class GoogleMapsView(context: Context, appContext: AppContext) :
     val cameraPosition = props.cameraPosition.value
     cameraState = remember(cameraPosition) {
       CameraPositionState(
-        position = CameraPosition.fromLatLngZoom(
+        position = CameraPosition(
           cameraPosition.coordinates.toLatLng(),
-          cameraPosition.zoom
+          cameraPosition.zoom,
+          cameraPosition.tilt,
+          cameraPosition.bearing
         )
       )
     }
@@ -369,8 +371,11 @@ class GoogleMapsView(context: Context, appContext: AppContext) :
       ?: props.userLocation.value.coordinates?.toLatLng()
       ?: return
 
-    val cameraUpdate = config?.zoom?.let { CameraUpdateFactory.newLatLngZoom(coordinates, it) }
-      ?: CameraUpdateFactory.newLatLng(coordinates)
+    val cameraUpdate = CameraUpdateFactory.newCameraPosition(
+      CameraPosition(coordinates, config?.zoom ?: cameraState.position.zoom,
+        config?.tilt ?: cameraState.position.tilt,
+        config?.bearing ?: cameraState.position.bearing)
+    )
 
     // When Int.MAX_VALUE is provided as durationMs, the default animation duration will be used.
     cameraState.animate(cameraUpdate, config?.duration ?: Int.MAX_VALUE)
diff --git a/android/src/main/java/expo/modules/maps/Records.kt b/android/src/main/java/expo/modules/maps/Records.kt
index 15854606398ea84a49d9eef55750eab4102aa174..f146579ec135fe40ac1404ffbb0e6a50567d9656 100644
--- a/android/src/main/java/expo/modules/maps/Records.kt
+++ b/android/src/main/java/expo/modules/maps/Records.kt
@@ -26,6 +26,12 @@ data class SetCameraPositionConfig(
   @Field
   val zoom: Float?,
 
+  @Field
+  val tilt: Float? = null,
+
+  @Field
+  val bearing: Float? = null,
+
   @Field
   val duration: Int?
 ) : Record
@@ -130,7 +136,13 @@ data class CameraPositionRecord(
   val coordinates: Coordinates = Coordinates(),
 
   @Field
-  val zoom: Float = 10f
+  val zoom: Float = 10f,
+
+  @Field
+  val tilt: Float = 0f,
+
+  @Field
+  val bearing: Float = 0f
 ) : Record
 
 @OptimizedRecord
diff --git a/build/shared.types.d.ts b/build/shared.types.d.ts
index 3a32344e3ef9b4c174c2878f01773fa912a61beb..239cb60bb9203e53ea4193a92e8c0e7d6df942d4 100644
--- a/build/shared.types.d.ts
+++ b/build/shared.types.d.ts
@@ -10,6 +10,8 @@ export type Coordinates = {
     longitude?: number;
 };
 export type CameraPosition = {
+  tilt?: number;
+  bearing?: number;
     /**
      * The middle point of the camera.
      */
diff --git a/ios/MapRecords.swift b/ios/MapRecords.swift
index 7cab76612a78a83ae93f2611e727cf5529acdb55..f89cd1ad1905f8a423300e2f24a01e2f81d9a92f 100644
--- a/ios/MapRecords.swift
+++ b/ios/MapRecords.swift
@@ -41,10 +41,13 @@ struct MapMarker: Identifiable, Record {
 struct CameraPosition: Record, Equatable {
   @Field var coordinates: Coordinate
   @Field var zoom: Double = 1
+  @Field var tilt: Double = 0
+  @Field var bearing: Double = 0
 
   static func == (lhs: CameraPosition, rhs: CameraPosition) -> Bool {
     return lhs.coordinates.latitude == rhs.coordinates.latitude
       && lhs.coordinates.longitude == rhs.coordinates.longitude && lhs.zoom == rhs.zoom
+      && lhs.tilt == rhs.tilt && lhs.bearing == rhs.bearing
   }
 }
 
diff --git a/ios/MapUtils.swift b/ios/MapUtils.swift
index b1bdee1319481da0c1c13eea2ab767eb52bea86d..46e46873a0c414a6deb06995969407896805ee69 100644
--- a/ios/MapUtils.swift
+++ b/ios/MapUtils.swift
@@ -7,6 +7,12 @@ func convertToMapCamera(position: CameraPosition) -> MapCameraPosition {
     latitude: position.coordinates.latitude,
     longitude: position.coordinates.longitude
   )
+  if position.tilt != 0 || position.bearing != 0 {
+    let span = 360 / pow(2, position.zoom)
+    let distance = max(1, span * 111_320 * cos(coordinate.latitude * .pi / 180))
+    return .camera(MapCamera(centerCoordinate: coordinate, distance: distance,
+      heading: position.bearing, pitch: position.tilt))
+  }
   return convertToMapCameraPosition(coordinate: coordinate, zoom: position.zoom)
 }
 
diff --git a/src/shared.types.ts b/src/shared.types.ts
index ce531a92279fde5776b013f96b80cc4d257c5540..b5bacc02e213f8bf16d2358ded28407f53f5ec88 100644
--- a/src/shared.types.ts
+++ b/src/shared.types.ts
@@ -13,6 +13,8 @@ export type Coordinates = {
 };
 
 export type CameraPosition = {
+  tilt?: number;
+  bearing?: number;
   /**
    * The middle point of the camera.
    */
